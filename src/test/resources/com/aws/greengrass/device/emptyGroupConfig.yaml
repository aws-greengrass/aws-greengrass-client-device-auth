---
services:
  main:
    lifecycle:
      install:
        all: echo All installed
    dependencies:
      - aws.greengrass.DeviceSupport

  aws.greengrass.DeviceSupport:
    configuration:
      deviceGroups: {}
