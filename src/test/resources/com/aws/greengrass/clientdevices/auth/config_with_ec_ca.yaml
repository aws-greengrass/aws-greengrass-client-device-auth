---
services:
  aws.greengrass.clientdevices.Auth:
    configuration:
      certificateAuthority:
        caType: [ "ECDSA_P256" ]
  main:
    lifecycle:
      install: ""
    dependencies:
      - aws.greengrass.clientdevices.Auth
