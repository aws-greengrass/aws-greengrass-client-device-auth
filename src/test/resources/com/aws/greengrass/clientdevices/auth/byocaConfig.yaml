---
services:
  aws.greengrass.clientdevices.Auth:
    configuration:
      certificateAuthority:
        certificateUri: "file://CERTIFICATE_URI"
        privateKeyUri: "file://PRIVATE_KEY_URI"

  main:
    lifecycle:
      install:
        all: echo All installed
    dependencies:
      - aws.greengrass.clientdevices.Auth
